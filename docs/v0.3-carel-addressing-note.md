# Carel test addressing note

During the mixed RTU + TCP integration test, the Carel test PLC exposed its cSuite address with zero-based numbering relative to the RutOS first-register field. The tested cSuite register 240 was read successfully in RutOS as first register 241. This is device-specific and is not applied globally by the configurator.
